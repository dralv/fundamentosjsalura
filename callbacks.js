const nomes = ['Ana', 'Andre', 'Marcio', 'Carlos'];

nomes.forEach(imprimeNome);

function imprimeNome(a){

    console.log(a)

}

// forEach serve para executar uma função para cada elemento de um array